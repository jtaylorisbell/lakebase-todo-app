# =============================================================================
# Lakebase Todo App - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
#
# Authentication is handled by the Databricks SDK via your CLI profile.
# Set the profile before running:
#   export DATABRICKS_CONFIG_PROFILE=my-profile
# =============================================================================

# -----------------------------------------------------------------------------
# LAKEBASE (POSTGRESQL) DATABASE CONNECTION
# -----------------------------------------------------------------------------
# The endpoint host is resolved automatically from the Lakebase API.
# Only the project/branch/endpoint IDs and database name are needed.
#
# Local development uses the "local-dev" branch to isolate from the
# deployed app (which uses "main" — configured in app.yaml).
LAKEBASE_PROJECT_ID=todo-app
LAKEBASE_BRANCH_ID=local-dev
LAKEBASE_ENDPOINT_ID=default
LAKEBASE_DATABASE=todoapp
# LAKEBASE_USER is auto-detected from your Databricks identity if not set.
# For local dev, set it to your email (must match your Lakebase Postgres role).
LAKEBASE_USER=your.email@company.com

# -----------------------------------------------------------------------------
# USER IDENTITY (LOCAL DEVELOPMENT ONLY)
# On Databricks Apps, identity comes from X-Forwarded-Email headers.
# -----------------------------------------------------------------------------
USER_EMAIL=developer@company.com
USER_NAME=Developer
